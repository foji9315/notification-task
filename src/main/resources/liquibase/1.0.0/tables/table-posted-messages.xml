<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <property name="tableName" value="posted_messages"/>

    <changeSet id="table-posted-messages-0" author="${AuthorName}">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists schemaName="${schemaName}" tableName="${tableName}" />
            </not>
        </preConditions>
        <createTable schemaName="${schemaName}" tableName="${tableName}">
            <column name="id" type="CHAR(36)" >
"                <constraints primaryKey="true" primaryKeyName="pk_message_id" nullable="false"/>
"            </column>
            <column name="message" type="VARCHAR(200)">
                <constraints nullable="false"/>
            </column>
            <column name="category" type="VARCHAR(20)">
                <constraints nullable="false" />
            </column>
            <column name="created_at" type="datetime2">
                <constraints nullable="false" />
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>